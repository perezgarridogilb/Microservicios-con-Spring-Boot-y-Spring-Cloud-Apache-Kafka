ARCHIVOS MODIFICADOS HOY — 2026-06-25
======================================

1. AuthServiceImpl.java
   Ruta: src/main/java/com/example/crudrapido/service/impl/AuthServiceImpl.java
   Cambios:
   - Cambiado import de `src/main/java/com/example/crudrapido/security/JwtUtil.java`
     a `com.example.crudrapido.security.JwtUtil` (import correcto de Java)
   - Agregados imports: JwtUtil, HttpStatus, PasswordEncoder, ResponseStatusException,
     Employee, Patient, Rol, EmployeeRepository, PatientRepository
   - Eliminados imports: MessageDigest, NoSuchAlgorithmException, Base64
   - Eliminado campo `AuthenticationManager` (ya no se usa)
   - Agregados campos: PatientRepository, EmployeeRepository, PasswordEncoder, JwtUtil
   - `registrar()`: cambiado `hashPassword()` por `passwordEncoder.encode()`
   - `login()`: ahora busca usuario por `findByUser`, verifica password con
     `passwordEncoder.matches()`, determina rol (PACIENTE o desde Employee),
     genera token JWT con `jwtUtil.generateToken()`
   - Eliminados métodos muertos: `hashPassword()`, `verifyPassword()`, `generateToken()`

2. SecurityConfig.java
   Ruta: src/main/java/com/example/crudrapido/security/SecurityConfig.java
   Cambios:
   - Agregado import: HttpMethod, DaoAuthenticationProvider
   - Agregado bean `authenticationProvider()` que crea un `DaoAuthenticationProvider`
     con el `CustomUserDetailsService` y `PasswordEncoder` (BCrypt)
   - El `authenticationManager` bean se mantiene con `AuthenticationConfiguration`

3. AtentionController.java
   Ruta: src/main/java/com/example/crudrapido/controller/AtentionController.java
   Cambios:
   - Agregados imports: PreAuthorize, Authentication, SecurityContext, SecurityContextHolder
   - Cambiado campo `StudentService patientService` a `PatientService patientService`
   - Agregado endpoint GET `/mias` con `@PreAuthorize("hasRole('PACIENTE')")`
     que llama a `atentionService.listarAtentionsDelPacienteAutenticado()`

4. application.properties
   Ruta: src/main/resources/application.properties
   Cambios:
   - Agregadas propiedades JWT:
     jwt.secret=ngOP0sYyzr5a+fdqiQ3A9m8GLkG0+k88IzAb+l+Gj/Y=
     jwt.expirationMs=86400000


ARCHIVOS NUEVOS (CREADOS AYER, SIN MODIFICAR HOY)
==================================================
Estos archivos ya existían en src/main/java/com/example/crudrapido/security/
(creados el 24 de junio, no fueron modificados hoy):

5. JwtUtil.java
   - Componente Spring con @Value para jwt.secret y jwt.expirationMs
   - generateToken(String nombre, Rol rol) — genera JWT con subject+rol
   - getUsuarioFromToken(String token) — extrae subject del token
   - validateToken(String token) — valida firma y expiración
   - getSigningKey() — construye SecretKey desde Base64

6. CustomUserDetailsService.java
   - Implementa UserDetailsService
   - loadUserByUsername(): busca User, determina rol (PACIENTE o desde Employee),
     retorna org.springframework.security.core.userdetails.User

7. JwtAuthenticationFilter.java
   - Extiende OncePerRequestFilter
   - doFilterInternal(): extrae token Bearer, valida, carga UserDetails,
     settea SecurityContext


ESTADO ACTUAL DE LA APLICACIÓN
===============================
- Compilación: OK (mvn compile exitoso)
- Runtime: El login de juanp2 falla porque student_id=2 no tiene registro
  en la tabla employees ni patients. Se necesita crear un Employee o Patient
  para ese estudiante.
- Endpoints disponibles:
  POST /api/auth/registrar  — Registrar usuario (BCrypt)
  POST /api/auth/login      — Login (retorna JWT)
  GET  /api/atenciones/mias — Atenciones del paciente autenticado
  POST /api/atenciones      — Crear atención
  GET  /api/atenciones      — Listar atenciones (paginado)
  GET  /api/atenciones/fecha — Por rango de fechas
  GET  /api/atenciones/{id}  — Por ID
  PUT  /api/atenciones/{id}  — Actualizar
  DELETE /api/atenciones/{id} — Eliminar
